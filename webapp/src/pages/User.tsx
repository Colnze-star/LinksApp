import TableLinks from '../widgets/TableLinks';
import CreateLinks from '../widgets/CreateLinks';

const User = () => {

  return (
    <> 
        <CreateLinks />
        <TableLinks />
    </>
 
  );
}

export default User